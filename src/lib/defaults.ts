import type { PageSections } from "./types";

export const defaultSections: PageSections = {
  showEventDate: true,
  showEventLocation: true,
  showDescription: true,
  showFeatures: true,
  showCta: true,
};
